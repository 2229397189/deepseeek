# 复制为 .env 后按需修改。不配 LLM_API_KEY 时服务会落到确定性实现（可离线联调）。
AGENT_HOST=127.0.0.1
AGENT_PORT=8000

AGENT_LLM_BASE_URL=https://api.deepseek.com/v1
AGENT_LLM_API_KEY=
AGENT_LLM_MODEL=deepseek-chat
AGENT_LLM_TIMEOUT_S=60

# 置为 true 可强制走确定性实现（演示 / 压测 / 断网）
AGENT_FORCE_MOCK=false
